<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>My Dashboard — eTicketSupport</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        :root {
            --bg:          #080c18;
            --surface:     #0f1424;
            --surface-2:   #161b2e;
            --surface-3:   #1c2238;
            --border:      rgba(255,255,255,0.07);
            --border-2:    rgba(255,255,255,0.12);
            --accent:      #4f8ef7;
            --accent-dim:  rgba(79,142,247,0.15);
            --accent-glow: rgba(79,142,247,0.25);
            --violet:      #7c3aed;
            --violet-dim:  rgba(124,58,237,0.15);
            --success:     #22c55e;
            --success-dim: rgba(34,197,94,0.12);
            --warning:     #f59e0b;
            --warning-dim: rgba(245,158,11,0.12);
            --danger:      #ef4444;
            --danger-dim:  rgba(239,68,68,0.12);
            --text:        #dde3f4;
            --text-2:      #9aa3bb;
            --muted:       #5a6278;
            --sidebar-w:   260px;
        }
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html { scroll-behavior: smooth; }
        body {
            font-family: 'DM Sans', sans-serif;
            background: var(--bg);
            color: var(--text);
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* ── SIDEBAR ── */
        .sidebar {
            position: fixed; left: 0; top: 0; bottom: 0;
            width: var(--sidebar-w);
            background: var(--surface);
            border-right: 1px solid var(--border);
            display: flex; flex-direction: column;
            z-index: 200;
            transition: transform .3s cubic-bezier(.4,0,.2,1);
        }
        .sidebar-logo {
            padding: 26px 22px 22px;
            border-bottom: 1px solid var(--border);
        }
        .logo-mark { display: flex; align-items: center; gap: 10px; }
        .logo-icon {
            width: 36px; height: 36px; border-radius: 10px;
            background: linear-gradient(135deg, var(--accent), var(--violet));
            display: flex; align-items: center; justify-content: center;
            font-size: .95rem; color: #fff;
            box-shadow: 0 0 20px var(--accent-glow);
            flex-shrink: 0;
        }
        .logo-text .brand {
            font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.05rem;
            background: linear-gradient(135deg, #ffffff 30%, #a78bfa);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
            letter-spacing: -.02em; display: block;
        }
        .logo-text .sub { font-size: .68rem; color: var(--muted); letter-spacing: .1em; text-transform: uppercase; }

        .sidebar-user {
            margin: 16px 12px 4px;
            padding: 14px;
            background: var(--surface-2);
            border: 1px solid var(--border);
            border-radius: 14px;
            display: flex; align-items: center; gap: 12px;
        }
        .user-avatar {
            width: 40px; height: 40px; border-radius: 12px;
            background: linear-gradient(135deg, var(--accent), var(--violet));
            display: flex; align-items: center; justify-content: center;
            font-family: 'Syne', sans-serif; font-weight: 700; font-size: .9rem;
            color: #fff; flex-shrink: 0;
        }
        .user-name { font-weight: 600; font-size: .875rem; line-height: 1.3; }
        .user-plan { font-size: .7rem; color: var(--muted); }

        .sidebar-nav { flex: 1; padding: 12px; overflow-y: auto; }
        .nav-label {
            font-size: .65rem; font-weight: 600; letter-spacing: .12em;
            text-transform: uppercase; color: var(--muted);
            padding: 0 12px; margin: 18px 0 6px;
        }
        .nav-item {
            display: flex; align-items: center; gap: 12px;
            padding: 10px 14px; border-radius: 10px;
            color: var(--muted); font-size: .875rem; font-weight: 500;
            text-decoration: none; cursor: pointer;
            transition: all .2s; position: relative;
        }
        .nav-item i { width: 18px; text-align: center; font-size: .9rem; }
        .nav-item:hover { color: var(--text); background: var(--surface-2); }
        .nav-item.active {
            color: var(--accent);
            background: var(--accent-dim);
        }
        .nav-item.active::before {
            content: ''; position: absolute; left: 0; top: 20%; bottom: 20%;
            width: 3px; background: var(--accent); border-radius: 0 3px 3px 0;
        }
        .nav-badge {
            margin-left: auto; background: var(--accent); color: #fff;
            font-size: .65rem; font-weight: 700; padding: 2px 7px;
            border-radius: 20px; min-width: 20px; text-align: center;
        }
        .nav-badge.warn { background: var(--warning); }

        .sidebar-footer {
            padding: 16px 12px;
            border-top: 1px solid var(--border);
        }
        .btn-new-ticket {
            display: flex; align-items: center; justify-content: center; gap: 8px;
            width: 100%; padding: 12px;
            background: linear-gradient(135deg, var(--accent), var(--violet));
            color: #fff; font-family: 'Syne', sans-serif;
            font-weight: 700; font-size: .875rem; border: none;
            border-radius: 12px; cursor: pointer; text-decoration: none;
            transition: opacity .2s, transform .15s;
            box-shadow: 0 4px 20px var(--accent-glow);
        }
        .btn-new-ticket:hover { opacity: .92; transform: translateY(-1px); }

        /* ── TOPBAR ── */
        .topbar {
            position: fixed; top: 0; left: var(--sidebar-w); right: 0;
            height: 64px;
            background: rgba(8,12,24,.85);
            backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--border);
            display: flex; align-items: center; gap: 16px;
            padding: 0 28px; z-index: 100;
        }
        .topbar-title {
            font-family: 'Syne', sans-serif; font-weight: 700;
            font-size: 1.05rem; flex: 1;
        }
        .topbar-title span { color: var(--muted); font-weight: 400; font-size: .875rem; margin-left: 6px; }
        .search-bar {
            display: flex; align-items: center; gap: 10px;
            background: var(--surface-2); border: 1px solid var(--border);
            border-radius: 10px; padding: 8px 14px;
            transition: border-color .2s;
        }
        .search-bar:focus-within { border-color: var(--accent); }
        .search-bar i { color: var(--muted); font-size: .85rem; }
        .search-bar input {
            background: none; border: none; outline: none;
            color: var(--text); font-family: 'DM Sans', sans-serif;
            font-size: .875rem; width: 220px;
        }
        .search-bar input::placeholder { color: var(--muted); }
        .topbar-actions { display: flex; align-items: center; gap: 10px; }
        .icon-btn {
            width: 38px; height: 38px; border-radius: 10px;
            background: var(--surface-2); border: 1px solid var(--border);
            display: flex; align-items: center; justify-content: center;
            color: var(--text-2); cursor: pointer; transition: all .2s;
            position: relative; font-size: .9rem;
        }
        .icon-btn:hover { border-color: var(--border-2); color: var(--text); }
        .notif-dot {
            position: absolute; top: 6px; right: 6px;
            width: 8px; height: 8px; border-radius: 50%;
            background: var(--danger); border: 2px solid var(--bg);
        }
        .mobile-toggle {
            display: none; background: none; border: none;
            color: var(--text); font-size: 1.1rem; cursor: pointer;
        }

        /* ── MAIN ── */
        .main {
            margin-left: var(--sidebar-w);
            padding: 88px 28px 40px;
            min-height: 100vh;
        }

        /* Welcome banner */
        .welcome-banner {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 18px;
            padding: 28px 32px;
            display: flex; align-items: center; justify-content: space-between;
            gap: 20px; margin-bottom: 28px;
            position: relative; overflow: hidden;
        }
        .welcome-banner::before {
            content: '';
            position: absolute; right: -60px; top: -60px;
            width: 240px; height: 240px; border-radius: 50%;
            background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
            pointer-events: none;
        }
        .welcome-banner::after {
            content: '';
            position: absolute; right: 80px; bottom: -80px;
            width: 180px; height: 180px; border-radius: 50%;
            background: radial-gradient(circle, var(--violet-dim) 0%, transparent 70%);
            pointer-events: none;
        }
        .welcome-text h2 {
            font-family: 'Syne', sans-serif; font-weight: 700;
            font-size: 1.4rem; margin-bottom: 6px;
        }
        .welcome-text h2 .name { color: var(--accent); }
        .welcome-text p { color: var(--text-2); font-size: .9rem; }
        .welcome-cta {
            display: flex; align-items: center; gap: 10px; flex-shrink: 0;
        }
        .btn-primary {
            display: inline-flex; align-items: center; gap: 8px;
            padding: 11px 22px;
            background: linear-gradient(135deg, var(--accent), var(--violet));
            color: #fff; font-weight: 600; font-size: .875rem;
            border: none; border-radius: 11px; cursor: pointer;
            text-decoration: none; transition: opacity .2s, transform .15s;
            box-shadow: 0 4px 16px var(--accent-glow);
        }
        .btn-primary:hover { opacity: .9; transform: translateY(-1px); }
        .btn-outline {
            display: inline-flex; align-items: center; gap: 8px;
            padding: 11px 22px;
            background: var(--surface-2); color: var(--text-2);
            font-weight: 500; font-size: .875rem;
            border: 1px solid var(--border); border-radius: 11px;
            cursor: pointer; text-decoration: none; transition: all .2s;
        }
        .btn-outline:hover { border-color: var(--border-2); color: var(--text); }

        /* Stats grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 16px; margin-bottom: 28px;
        }
        .stat-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 22px;
            position: relative; overflow: hidden;
            transition: border-color .2s, transform .2s;
        }
        .stat-card:hover { border-color: var(--border-2); transform: translateY(-2px); }
        .stat-card .glow {
            position: absolute; top: -30px; right: -30px;
            width: 100px; height: 100px; border-radius: 50%;
            pointer-events: none;
        }
        .stat-icon {
            width: 40px; height: 40px; border-radius: 11px;
            display: flex; align-items: center; justify-content: center;
            font-size: .95rem; margin-bottom: 16px;
        }
        .stat-icon.blue  { background: var(--accent-dim); color: var(--accent); }
        .stat-icon.amber { background: var(--warning-dim); color: var(--warning); }
        .stat-icon.green { background: var(--success-dim); color: var(--success); }
        .stat-icon.violet{ background: var(--violet-dim);  color: #a78bfa; }
        .stat-value {
            font-family: 'Syne', sans-serif; font-weight: 700;
            font-size: 2rem; line-height: 1; margin-bottom: 4px;
        }
        .stat-label { color: var(--muted); font-size: .8rem; font-weight: 500; }
        .stat-trend {
            margin-top: 10px; font-size: .78rem; font-weight: 500;
            display: flex; align-items: center; gap: 4px;
        }
        .stat-trend.up { color: var(--success); }
        .stat-trend.down { color: var(--danger); }

        /* Content row */
        .content-row {
            display: grid; grid-template-columns: 1fr 340px;
            gap: 20px; margin-bottom: 28px;
        }

        /* Tickets table card */
        .card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 18px;
            overflow: hidden;
        }
        .card-header {
            padding: 20px 24px;
            border-bottom: 1px solid var(--border);
            display: flex; align-items: center; justify-content: space-between;
        }
        .card-title {
            font-family: 'Syne', sans-serif;
            font-weight: 700; font-size: .95rem;
        }
        .card-title span { color: var(--muted); font-family: 'DM Sans',sans-serif; font-weight: 400; font-size: .8rem; margin-left: 8px; }
        .card-action { font-size: .8rem; color: var(--accent); text-decoration: none; font-weight: 500; }
        .card-action:hover { text-decoration: underline; }

        /* Filter tabs */
        .filter-tabs {
            display: flex; gap: 4px;
            padding: 12px 24px;
            border-bottom: 1px solid var(--border);
            background: var(--surface-2);
        }
        .filter-tab {
            padding: 6px 14px; border-radius: 8px;
            font-size: .8rem; font-weight: 500;
            color: var(--muted); cursor: pointer; border: none;
            background: none; transition: all .2s;
        }
        .filter-tab:hover { color: var(--text); background: var(--surface-3); }
        .filter-tab.active { color: var(--accent); background: var(--accent-dim); }

        /* Tickets table */
        .tickets-table { width: 100%; border-collapse: collapse; }
        .tickets-table th {
            padding: 12px 24px; text-align: left;
            font-size: .72rem; font-weight: 600;
            color: var(--muted); letter-spacing: .08em;
            text-transform: uppercase;
            background: var(--surface-2);
            border-bottom: 1px solid var(--border);
        }
        .tickets-table td { padding: 16px 24px; border-bottom: 1px solid var(--border); vertical-align: middle; }
        .tickets-table tr:last-child td { border-bottom: none; }
        .tickets-table tr:hover td { background: var(--surface-2); }
        .ticket-id { font-family: 'Syne', sans-serif; font-weight: 700; font-size: .8rem; color: var(--accent); }
        .ticket-subject { font-weight: 500; font-size: .875rem; display: block; margin-bottom: 3px; }
        .ticket-category { font-size: .75rem; color: var(--muted); }
        .ticket-date { font-size: .8rem; color: var(--text-2); }

        /* Status & Priority badges */
        .badge {
            display: inline-flex; align-items: center; gap: 5px;
            padding: 4px 10px; border-radius: 20px;
            font-size: .72rem; font-weight: 600; letter-spacing: .02em;
        }
        .badge::before { content: ''; width: 6px; height: 6px; border-radius: 50%; }
        .badge-open    { background: var(--accent-dim);   color: var(--accent);   }
        .badge-open::before    { background: var(--accent); }
        .badge-pending { background: var(--warning-dim);  color: var(--warning);  }
        .badge-pending::before { background: var(--warning); }
        .badge-closed  { background: var(--success-dim);  color: var(--success);  }
        .badge-closed::before  { background: var(--success); }
        .badge-urgent  { background: var(--danger-dim);   color: var(--danger);   }
        .badge-urgent::before  { background: var(--danger); }
        .badge-high    { background: var(--warning-dim);  color: var(--warning);  }
        .badge-high::before    { background: var(--warning); }
        .badge-normal  { background: var(--surface-3);    color: var(--text-2);   }
        .badge-normal::before  { background: var(--muted); }

        .btn-view {
            padding: 6px 14px; border-radius: 8px;
            background: var(--surface-3); border: 1px solid var(--border);
            color: var(--text-2); font-size: .78rem; font-weight: 500;
            cursor: pointer; transition: all .2s; text-decoration: none;
            display: inline-block;
        }
        .btn-view:hover { border-color: var(--accent); color: var(--accent); }

        /* Sidebar panels */
        .panel { padding: 20px 24px; }

        /* Activity feed */
        .activity-list { list-style: none; }
        .activity-item {
            display: flex; gap: 12px; padding: 14px 0;
            border-bottom: 1px solid var(--border);
        }
        .activity-item:last-child { border-bottom: none; }
        .activity-icon {
            width: 34px; height: 34px; border-radius: 10px;
            display: flex; align-items: center; justify-content: center;
            font-size: .8rem; flex-shrink: 0; margin-top: 2px;
        }
        .activity-icon.reply  { background: var(--accent-dim); color: var(--accent); }
        .activity-icon.status { background: var(--success-dim); color: var(--success); }
        .activity-icon.warn   { background: var(--warning-dim); color: var(--warning); }
        .activity-icon.create { background: var(--violet-dim); color: #a78bfa; }
        .activity-body .msg { font-size: .85rem; line-height: 1.5; }
        .activity-body .msg strong { color: var(--accent); }
        .activity-body .time { font-size: .75rem; color: var(--muted); margin-top: 3px; }

        /* Knowledge base quick links */
        .kb-list { list-style: none; }
        .kb-item {
            display: flex; align-items: center; gap: 12px;
            padding: 12px 0; border-bottom: 1px solid var(--border);
            text-decoration: none; color: var(--text-2);
            font-size: .875rem; transition: color .2s;
        }
        .kb-item:last-child { border-bottom: none; }
        .kb-item i { color: var(--accent); width: 16px; text-align: center; }
        .kb-item:hover { color: var(--text); }
        .kb-item:hover i { color: #a78bfa; }

        /* Bottom row */
        .bottom-row {
            display: grid; grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        /* Empty state */
        .empty-state {
            display: flex; flex-direction: column; align-items: center;
            justify-content: center; padding: 40px 20px; text-align: center;
        }
        .empty-state i { font-size: 2.5rem; color: var(--muted); margin-bottom: 14px; }
        .empty-state p { color: var(--muted); font-size: .9rem; }

        /* Satisfaction section */
        .satisfaction-row { display: flex; gap: 12px; flex-wrap: wrap; }
        .rating-btn {
            flex: 1; min-width: 80px;
            padding: 14px 8px;
            background: var(--surface-2); border: 1px solid var(--border);
            border-radius: 12px; cursor: pointer; transition: all .2s;
            display: flex; flex-direction: column; align-items: center; gap: 6px;
            color: var(--muted); font-size: .78rem; font-weight: 500;
        }
        .rating-btn i { font-size: 1.4rem; }
        .rating-btn:hover { border-color: var(--border-2); color: var(--text); }
        .rating-btn.great:hover, .rating-btn.great.active { border-color: var(--success); color: var(--success); background: var(--success-dim); }
        .rating-btn.okay:hover,  .rating-btn.okay.active  { border-color: var(--warning); color: var(--warning); background: var(--warning-dim); }
        .rating-btn.poor:hover,  .rating-btn.poor.active  { border-color: var(--danger);  color: var(--danger);  background: var(--danger-dim); }

        /* Pagination */
        .pagination {
            display: flex; align-items: center; justify-content: space-between;
            padding: 16px 24px;
            border-top: 1px solid var(--border);
        }
        .pagination-info { font-size: .8rem; color: var(--muted); }
        .pagination-btns { display: flex; gap: 6px; }
        .page-btn {
            width: 34px; height: 34px; border-radius: 8px;
            background: var(--surface-2); border: 1px solid var(--border);
            color: var(--text-2); font-size: .82rem; font-weight: 500;
            cursor: pointer; display: flex; align-items: center; justify-content: center;
            transition: all .2s;
        }
        .page-btn:hover { border-color: var(--border-2); color: var(--text); }
        .page-btn.active { background: var(--accent-dim); border-color: var(--accent); color: var(--accent); }

        /* Toast */
        .toast-container {
            position: fixed; bottom: 24px; right: 24px;
            display: flex; flex-direction: column; gap: 10px;
            z-index: 9000;
        }
        .toast {
            display: flex; align-items: center; gap: 12px;
            background: var(--surface-2); border: 1px solid var(--border-2);
            border-radius: 14px; padding: 14px 18px;
            font-size: .875rem; box-shadow: 0 8px 32px rgba(0,0,0,.4);
            animation: slideUp .3s ease forwards;
            max-width: 320px;
        }
        .toast i { font-size: 1rem; }
        .toast.success { border-color: rgba(34,197,94,.3); }
        .toast.success i { color: var(--success); }
        @keyframes slideUp {
            from { transform: translateY(20px); opacity: 0; }
            to   { transform: translateY(0);    opacity: 1; }
        }

        /* Mobile overlay */
        .sidebar-overlay {
            display: none; position: fixed; inset: 0;
            background: rgba(0,0,0,.6); z-index: 150;
        }

        /* ── RESPONSIVE ── */
        @media (max-width: 1280px) {
            .stats-grid { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 1024px) {
            .content-row { grid-template-columns: 1fr; }
            .bottom-row  { grid-template-columns: 1fr; }
        }
        @media (max-width: 768px) {
            .sidebar { transform: translateX(-100%); }
            .sidebar.open { transform: translateX(0); }
            .sidebar-overlay.open { display: block; }
            .main { margin-left: 0; padding: 76px 16px 32px; }
            .topbar { left: 0; padding: 0 16px; }
            .mobile-toggle { display: block; }
            .search-bar { display: none; }
            .stats-grid { grid-template-columns: 1fr 1fr; }
            .welcome-cta { display: none; }
        }
        @media (max-width: 480px) {
            .stats-grid { grid-template-columns: 1fr; }
            .tickets-table th:nth-child(3),
            .tickets-table td:nth-child(3),
            .tickets-table th:nth-child(5),
            .tickets-table td:nth-child(5) { display: none; }
        }

        /* Entrance animations */
        .fade-up {
            opacity: 0;
            animation: fadeUp .45s ease forwards;
        }
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to   { opacity: 1; transform: translateY(0); }
        }
        .fade-up:nth-child(1) { animation-delay: .05s; }
        .fade-up:nth-child(2) { animation-delay: .12s; }
        .fade-up:nth-child(3) { animation-delay: .19s; }
        .fade-up:nth-child(4) { animation-delay: .26s; }
        .fade-up:nth-child(5) { animation-delay: .33s; }
        .fade-up:nth-child(6) { animation-delay: .40s; }
    </style>
</head>
<body>

{{-- ══════════════ SIDEBAR ══════════════ --}}
<div class="sidebar-overlay" id="sidebarOverlay"></div>
<aside class="sidebar" id="sidebar">
    <div class="sidebar-logo">
        <div class="logo-mark">
            <div class="logo-icon"><i class="fa-solid fa-ticket"></i></div>
            <div class="logo-text">
                <span class="brand">eTicketSupport</span>
                <span class="sub">Help Center</span>
            </div>
        </div>
    </div>

    <div class="sidebar-user">
        <div class="user-avatar">{{ strtoupper(substr(auth()->user()->name, 0, 2)) }}</div>
        <div>
            <div class="user-name">{{ auth()->user()->name }}</div>
            <div class="user-plan">{{ auth()->user()->plan ?? 'Standard Plan' }}</div>
        </div>
    </div>

    <nav class="sidebar-nav">
        <div class="nav-label">Main</div>
        <a href="{{ route('user.dashboard') }}" class="nav-item active">
            <i class="fa-solid fa-house"></i> Dashboard
        </a>
        <a href="{{ route('user.tickets') }}" class="nav-item">
            <i class="fa-solid fa-ticket"></i> My Tickets
            @if(isset($openCount) && $openCount > 0)
                <span class="nav-badge">{{ $openCount }}</span>
            @endif
        </a>
        <a href="{{ route('user.tickets.create') }}" class="nav-item">
            <i class="fa-solid fa-plus-circle"></i> New Ticket
        </a>

        <div class="nav-label">Resources</div>
        <a href="{{ route('user.knowledge-base') }}" class="nav-item">
            <i class="fa-solid fa-book-open"></i> Knowledge Base
        </a>
        <a href="{{ route('user.announcements') }}" class="nav-item">
            <i class="fa-solid fa-bullhorn"></i> Announcements
            @if(isset($unreadAnnouncements) && $unreadAnnouncements > 0)
                <span class="nav-badge warn">{{ $unreadAnnouncements }}</span>
            @endif
        </a>

        <div class="nav-label">Account</div>
        <a href="{{ route('user.profile') }}" class="nav-item">
            <i class="fa-solid fa-user"></i> Profile
        </a>
        <a href="{{ route('user.settings') }}" class="nav-item">
            <i class="fa-solid fa-gear"></i> Settings
        </a>
    </nav>

    <div class="sidebar-footer">
        <a href="{{ route('user.tickets.create') }}" class="btn-new-ticket">
            <i class="fa-solid fa-plus"></i> Submit New Ticket
        </a>
    </div>
</aside>

{{-- ══════════════ TOPBAR ══════════════ --}}
<header class="topbar">
    <button class="mobile-toggle" id="mobileToggle">
        <i class="fa-solid fa-bars"></i>
    </button>
    <div class="topbar-title">
        Dashboard <span>/ Overview</span>
    </div>
    <div class="search-bar">
        <i class="fa-solid fa-magnifying-glass"></i>
        <input type="text" id="searchInput" placeholder="Search tickets, articles…">
    </div>
    <div class="topbar-actions">
        <div class="icon-btn" title="Notifications">
            <i class="fa-solid fa-bell"></i>
            <span class="notif-dot"></span>
        </div>
        <div class="icon-btn" title="Help">
            <i class="fa-solid fa-circle-question"></i>
        </div>
        <form method="POST" action="{{ route('logout') }}">
            @csrf
            <button type="submit" class="icon-btn" title="Sign out" style="font-size:.85rem;">
                <i class="fa-solid fa-right-from-bracket"></i>
            </button>
        </form>
    </div>
</header>

{{-- ══════════════ MAIN CONTENT ══════════════ --}}
<main class="main">

    {{-- Flash messages --}}
    @if(session('success'))
    <div class="toast-container">
        <div class="toast success">
            <i class="fa-solid fa-circle-check"></i>
            <span>{{ session('success') }}</span>
        </div>
    </div>
    @endif

    {{-- Welcome banner --}}
    <div class="welcome-banner fade-up">
        <div class="welcome-text">
            <h2>Welcome back, <span class="name">{{ auth()->user()->name }}</span> 👋</h2>
            <p>You have
                @if(isset($pendingReplies) && $pendingReplies > 0)
                    <strong>{{ $pendingReplies }} ticket{{ $pendingReplies > 1 ? 's' : '' }}</strong> awaiting your reply.
                @else
                    no pending actions — you're all caught up!
                @endif
            </p>
        </div>
        <div class="welcome-cta">
            <a href="{{ route('user.knowledge-base') }}" class="btn-outline">
                <i class="fa-solid fa-book-open"></i> Browse KB
            </a>
            <a href="{{ route('user.tickets.create') }}" class="btn-primary">
                <i class="fa-solid fa-plus"></i> Open Ticket
            </a>
        </div>
    </div>

    {{-- Stats --}}
    <div class="stats-grid">
        <div class="stat-card fade-up">
            <div class="glow" style="background:radial-gradient(circle,var(--accent-glow),transparent 70%)"></div>
            <div class="stat-icon blue"><i class="fa-solid fa-ticket"></i></div>
            <div class="stat-value">{{ $totalTickets ?? 0 }}</div>
            <div class="stat-label">Total Tickets</div>
            <div class="stat-trend up"><i class="fa-solid fa-arrow-up"></i> All time</div>
        </div>
        <div class="stat-card fade-up">
            <div class="glow" style="background:radial-gradient(circle,rgba(245,158,11,.18),transparent 70%)"></div>
            <div class="stat-icon amber"><i class="fa-solid fa-clock"></i></div>
            <div class="stat-value">{{ $openTickets ?? 0 }}</div>
            <div class="stat-label">Open / In Progress</div>
            <div class="stat-trend {{ ($openTickets ?? 0) > 3 ? 'down' : 'up' }}">
                <i class="fa-solid fa-circle-dot"></i> Active now
            </div>
        </div>
        <div class="stat-card fade-up">
            <div class="glow" style="background:radial-gradient(circle,var(--success-dim),transparent 70%)"></div>
            <div class="stat-icon green"><i class="fa-solid fa-circle-check"></i></div>
            <div class="stat-value">{{ $closedTickets ?? 0 }}</div>
            <div class="stat-label">Resolved</div>
            <div class="stat-trend up"><i class="fa-solid fa-arrow-up"></i> Total resolved</div>
        </div>
        <div class="stat-card fade-up">
            <div class="glow" style="background:radial-gradient(circle,var(--violet-dim),transparent 70%)"></div>
            <div class="stat-icon violet"><i class="fa-solid fa-star"></i></div>
            <div class="stat-value">{{ $satisfactionScore ?? '—' }}</div>
            <div class="stat-label">Satisfaction Score</div>
            <div class="stat-trend up"><i class="fa-solid fa-heart"></i> Based on ratings</div>
        </div>
    </div>

    {{-- Tickets + Activity --}}
    <div class="content-row">
        {{-- Recent Tickets --}}
        <div class="card fade-up">
            <div class="card-header">
                <div class="card-title">My Tickets <span>Recent activity</span></div>
                <a href="{{ route('user.tickets') }}" class="card-action">View all →</a>
            </div>
            <div class="filter-tabs">
                <button class="filter-tab active" data-filter="all">All</button>
                <button class="filter-tab" data-filter="open">Open</button>
                <button class="filter-tab" data-filter="pending">Pending</button>
                <button class="filter-tab" data-filter="closed">Closed</button>
            </div>
            <table class="tickets-table">
                <thead>
                    <tr>
                        <th>Ticket ID</th>
                        <th>Subject</th>
                        <th>Priority</th>
                        <th>Status</th>
                        <th>Updated</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @forelse($tickets ?? [] as $ticket)
                    <tr data-status="{{ strtolower($ticket->status) }}">
                        <td><span class="ticket-id">#{{ str_pad($ticket->id, 5, '0', STR_PAD_LEFT) }}</span></td>
                        <td>
                            <span class="ticket-subject">{{ Str::limit($ticket->subject, 40) }}</span>
                            <span class="ticket-category">{{ $ticket->category->name ?? 'General' }}</span>
                        </td>
                        <td>
                            @php $p = strtolower($ticket->priority ?? 'normal'); @endphp
                            <span class="badge badge-{{ $p }}">{{ ucfirst($p) }}</span>
                        </td>
                        <td>
                            @php $s = strtolower($ticket->status); @endphp
                            <span class="badge badge-{{ $s }}">{{ ucfirst($s) }}</span>
                        </td>
                        <td class="ticket-date">{{ $ticket->updated_at->diffForHumans() }}</td>
                        <td><a href="{{ route('user.tickets.show', $ticket) }}" class="btn-view">View</a></td>
                    </tr>
                    @empty
                    <tr>
                        <td colspan="6">
                            <div class="empty-state">
                                <i class="fa-regular fa-folder-open"></i>
                                <p>No tickets yet. Submit one to get started!</p>
                            </div>
                        </td>
                    </tr>
                    @endforelse
                </tbody>
            </table>
            @if(isset($tickets) && $tickets->hasPages())
            <div class="pagination">
                <span class="pagination-info">
                    Showing {{ $tickets->firstItem() }}–{{ $tickets->lastItem() }} of {{ $tickets->total() }}
                </span>
                <div class="pagination-btns">
                    @if($tickets->onFirstPage())
                        <button class="page-btn" disabled><i class="fa-solid fa-chevron-left"></i></button>
                    @else
                        <a href="{{ $tickets->previousPageUrl() }}" class="page-btn"><i class="fa-solid fa-chevron-left"></i></a>
                    @endif
                    @foreach($tickets->getUrlRange(1, $tickets->lastPage()) as $page => $url)
                        <a href="{{ $url }}" class="page-btn {{ $page == $tickets->currentPage() ? 'active' : '' }}">{{ $page }}</a>
                    @endforeach
                    @if($tickets->hasMorePages())
                        <a href="{{ $tickets->nextPageUrl() }}" class="page-btn"><i class="fa-solid fa-chevron-right"></i></a>
                    @else
                        <button class="page-btn" disabled><i class="fa-solid fa-chevron-right"></i></button>
                    @endif
                </div>
            </div>
            @endif
        </div>

        {{-- Recent Activity --}}
        <div>
            <div class="card fade-up" style="margin-bottom:20px">
                <div class="card-header">
                    <div class="card-title">Recent Activity</div>
                </div>
                <div class="panel" style="padding:0 24px">
                    <ul class="activity-list">
                        @forelse($activities ?? [] as $activity)
                        <li class="activity-item">
                            <div class="activity-icon {{ $activity->icon_class ?? 'reply' }}">
                                <i class="fa-solid fa-{{ $activity->icon ?? 'reply' }}"></i>
                            </div>
                            <div class="activity-body">
                                <div class="msg">{!! $activity->description !!}</div>
                                <div class="time">{{ $activity->created_at->diffForHumans() }}</div>
                            </div>
                        </li>
                        @empty
                        <li class="activity-item">
                            <div class="activity-icon create"><i class="fa-solid fa-sparkles"></i></div>
                            <div class="activity-body">
                                <div class="msg">No recent activity yet.</div>
                                <div class="time">Submit a ticket to get started</div>
                            </div>
                        </li>
                        @endforelse
                    </ul>
                </div>
            </div>

            {{-- Knowledge Base quick links --}}
            <div class="card fade-up">
                <div class="card-header">
                    <div class="card-title">Quick Help</div>
                    <a href="{{ route('user.knowledge-base') }}" class="card-action">All articles →</a>
                </div>
                <div class="panel" style="padding:0 24px">
                    <ul class="kb-list">
                        @forelse($kbArticles ?? [] as $article)
                        <li>
                            <a href="{{ route('user.knowledge-base.show', $article) }}" class="kb-item">
                                <i class="fa-solid fa-file-lines"></i>
                                {{ Str::limit($article->title, 45) }}
                            </a>
                        </li>
                        @empty
                        <li><a href="{{ route('user.knowledge-base') }}" class="kb-item"><i class="fa-solid fa-book-open"></i> Browse our Knowledge Base</a></li>
                        <li><a href="{{ route('user.tickets.create') }}" class="kb-item"><i class="fa-solid fa-circle-plus"></i> How to submit a ticket</a></li>
                        <li><a href="{{ route('user.profile') }}" class="kb-item"><i class="fa-solid fa-user-gear"></i> Manage your account</a></li>
                        @endforelse
                    </ul>
                </div>
            </div>
        </div>
    </div>

    {{-- Bottom row --}}
    <div class="bottom-row">
        {{-- Rate Support --}}
        <div class="card fade-up">
            <div class="card-header">
                <div class="card-title">Rate Our Support</div>
            </div>
            <div class="panel">
                <p style="color:var(--text-2); font-size:.875rem; margin-bottom:18px;">
                    How satisfied are you with our support so far?
                </p>
                <div class="satisfaction-row">
                    <button class="rating-btn great" data-rating="great" onclick="rate(this)">
                        <i class="fa-solid fa-face-smile-beam"></i> Great
                    </button>
                    <button class="rating-btn okay" data-rating="okay" onclick="rate(this)">
                        <i class="fa-solid fa-face-meh"></i> Okay
                    </button>
                    <button class="rating-btn poor" data-rating="poor" onclick="rate(this)">
                        <i class="fa-solid fa-face-frown"></i> Poor
                    </button>
                </div>
            </div>
        </div>

        {{-- SLA Info / Plan --}}
        <div class="card fade-up">
            <div class="card-header">
                <div class="card-title">Support Plan</div>
                <a href="{{ route('user.profile') }}" class="card-action">Manage →</a>
            </div>
            <div class="panel">
                <div style="display:flex; flex-direction:column; gap:14px;">
                    <div style="display:flex; justify-content:space-between; align-items:center;">
                        <span style="font-size:.85rem; color:var(--text-2);">Current Plan</span>
                        <span style="font-family:'Syne',sans-serif; font-weight:700; color:var(--accent);">
                            {{ auth()->user()->plan ?? 'Standard' }}
                        </span>
                    </div>
                    <div style="display:flex; justify-content:space-between; align-items:center;">
                        <span style="font-size:.85rem; color:var(--text-2);">Response SLA</span>
                        <span style="font-size:.875rem; font-weight:600;">{{ $sla ?? '24 hours' }}</span>
                    </div>
                    <div style="display:flex; justify-content:space-between; align-items:center;">
                        <span style="font-size:.85rem; color:var(--text-2);">Priority Support</span>
                        <span class="badge {{ isset($prioritySupport) && $prioritySupport ? 'badge-closed' : 'badge-normal' }}">
                            {{ isset($prioritySupport) && $prioritySupport ? 'Enabled' : 'Upgrade' }}
                        </span>
                    </div>
                    <div style="height:1px; background:var(--border);"></div>
                    <div style="display:flex; justify-content:space-between; align-items:center;">
                        <span style="font-size:.85rem; color:var(--text-2);">Tickets this month</span>
                        <span style="font-size:.875rem; font-weight:600;">
                            {{ $monthlyTickets ?? 0 }} / {{ $ticketLimit ?? '∞' }}
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>

</main>

{{-- Toast container for JS --}}
<div class="toast-container" id="toastContainer"></div>

<script>
    // ── Mobile sidebar toggle ──
    const sidebar        = document.getElementById('sidebar');
    const overlay        = document.getElementById('sidebarOverlay');
    const mobileToggle   = document.getElementById('mobileToggle');

    mobileToggle.addEventListener('click', () => {
        sidebar.classList.toggle('open');
        overlay.classList.toggle('open');
    });
    overlay.addEventListener('click', () => {
        sidebar.classList.remove('open');
        overlay.classList.remove('open');
    });

    // ── Ticket filter tabs ──
    document.querySelectorAll('.filter-tab').forEach(btn => {
        btn.addEventListener('click', function () {
            document.querySelectorAll('.filter-tab').forEach(b => b.classList.remove('active'));
            this.classList.add('active');
            const filter = this.dataset.filter;
            document.querySelectorAll('.tickets-table tbody tr[data-status]').forEach(row => {
                row.style.display = (filter === 'all' || row.dataset.status === filter) ? '' : 'none';
            });
        });
    });

    // ── Client-side ticket search ──
    const searchInput = document.getElementById('searchInput');
    if (searchInput) {
        searchInput.addEventListener('input', function () {
            const q = this.value.toLowerCase();
            document.querySelectorAll('.tickets-table tbody tr[data-status]').forEach(row => {
                row.style.display = row.textContent.toLowerCase().includes(q) ? '' : 'none';
            });
        });
    }

    // ── Satisfaction rating ──
    function rate(btn) {
        document.querySelectorAll('.rating-btn').forEach(b => b.classList.remove('active'));
        btn.classList.add('active');

        fetch('{{ route("user.satisfaction.store") }}', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content
            },
            body: JSON.stringify({ rating: btn.dataset.rating })
        })
        .then(res => res.json())
        .then(() => showToast('Thanks for your feedback!', 'success'))
        .catch(() => {});
    }

    // ── Toast helper ──
    function showToast(msg, type = 'success') {
        const container = document.getElementById('toastContainer');
        const toast = document.createElement('div');
        toast.className = `toast ${type}`;
        toast.innerHTML = `<i class="fa-solid fa-circle-check"></i><span>${msg}</span>`;
        container.appendChild(toast);
        setTimeout(() => toast.remove(), 4000);
    }

    // ── Auto-dismiss session flash toasts ──
    document.querySelectorAll('.toast-container .toast').forEach(t => {
        setTimeout(() => t.remove(), 4000);
    });
</script>
</body>
</html>
